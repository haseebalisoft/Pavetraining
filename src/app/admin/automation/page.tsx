import { AdminPlaceholder } from "@/components/admin/AdminPlaceholder";

export default function AdminAutomationPage() {
  return (
    <AdminPlaceholder
      title="Automation"
      description="Automation rules and sync controls are not connected yet. Coming next."
    />
  );
}
